javascript: let playervideo = document.querySelectorAll("iframe");
if (window.location.href.includes("liga-1-max-pe-jok")) {
  window.location.replace("https://absimile.com/liga1m.php?id=live1");
};
if (!window.location.href.includes("/liga1m.php?id=live1")) {
  let frameElement;
  playervideo.forEach((fr) => {
    if (fr.frameBorder == "0" && fr.scrolling == "no") {
      frameElement = fr;
    } else {
      fr.remove();
    };
  });
  document.body.innerHTML = "";
  document.body.style.backgroundColor = "#000000";
  frameElement.style.display = "block";
  frameElement.style.height = window.screen.height.toString() + "px";
  frameElement.style.width = ((window.screen.height / 9) * 16).toString() + "px";
  frameElement.style.marginLeft = "auto";
  frameElement.style.marginRight = "auto";
  function deleteAdds() {
    let listA = document.querySelectorAll("a");
    listA.forEach((e) => {
      e.remove();
    });
    playervideo = document.querySelectorAll("iframe");
    playervideo.forEach((e) => {
      if (e.frameBorder != "0") e.remove();
    });
  };
  document.addEventListener("change", () => {
    let aElement = document.querySelectorAll("a");
    aElement.forEach((e) => {
      e.remove();
    });
  });
  deleteAdds();
  document.body.appendChild(frameElement);
  deleteAdds();
  // Get a reference to the last interval + 1
  const interval_id = window.setInterval(function () {},
  Number.MAX_SAFE_INTEGER);

  // Clear any timeout/interval up to that id
  for (let i = 1; i < interval_id; i++) {
    window.clearInterval(i);
  };
  document.querySelectorAll("body > div").forEach((e) => e.remove());
  window.open = null;
} else {
  document.querySelector("#myElement").style.position='static';
};
